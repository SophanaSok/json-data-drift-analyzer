{
  "id": "hawaii-hands",
  "sourceUrl": "https://hands.ehawaii.gov/hands/opportunities",
  "displayName": "0-State of Hawaii Multi-Juris-02",
  "detection": {
    "identityValues": {
      "AgentID": [
        "2051"
      ],
      "AgentName": [
        "0-State of Hawaii Multi-Juris-02"
      ]
    },
    "urlFields": [
      "ResourceURL"
    ],
    "urlPrefixes": [
      "https://hands.ehawaii.gov/hands/opportunities"
    ]
  },
  "version": 1,
  "primaryKey": [
    "AgentID",
    "ProjectCode"
  ],
  "fallbackKeys": [
    [
      "AgentID",
      "BidURL"
    ]
  ],
  "dedupeKey": [
    "AgentID",
    "ProjectCode"
  ],
  "hardRequiredFields": [
    "AgentID",
    "ProjectCode",
    "BidURL"
  ],
  "safeBackfillFields": [],
  "notes": [
    "STATUS: UNAPPROVED at v1. No field is approved for automatic backfill; per AGENTS.md rule 4 a field becomes backfillable only when a per-source decision adds it to safeBackfillFields (rule 6 fields additionally need an explicit approval note), accompanied by a version bump. Everything below is measured from the export drops named here, not assumed.",
    "IDENTITY: AgentID \"2051\" and AgentName \"0-State of Hawaii Multi-Juris-02\" are constant across 368/368 records in lambda-20260825-082059-8daa981a_2026-08-25.json, lambda-20260825-202507-c7532c38_2026-08-25.json. Detection ANDs both (see the base detection design); AgentID alone is not unique across the fleet — 1234 is carried by two bots.",
    "FACT — SHAPE: root {\"Export\": [...]}, all 45 fields present in 100% of records, every value a JSON string, UTF-8 BOM present. sourceUrl is the constant ResourceURL with volatile query parameters (tmstmp, session state) removed.",
    "FACT — KEYS (lambda-20260825-202507-c7532c38_2026-08-25.json, 178 records): BidURL non-blank 178/178, distinct 170; ProjectCode non-blank 178/178, distinct 178; LegacyAgentID constant.",
    "FACT — SHARED BidURL: 170 distinct BidURLs over 178 records — three HANDS opportunity pages each list several solicitations with distinct ProjectCodes (e.g. RFI HMS-501-27-01, B27000232, RFP NO. 460-27-01 all point at opportunity-details/27418). ProjectCode is 100% populated and distinct, so it is the primary and dedupe key; BidURL is the fallback and, because it is not unique, a fallback match can be ambiguous — the engine then emits a review candidate rather than a backfill (rule 4). BidURL hosts split hands.ehawaii.gov / hiepro.ehawaii.gov.",
    "FACT — PAIR: lambda-20260825-082059-8daa981a_2026-08-25.json (190) vs lambda-20260825-202507-c7532c38_2026-08-25.json (178) on ['AgentID', 'ProjectCode']: 176 shared, 14 reference-only, 2 candidate-only, match rate 0.926. minimumMatchRate is inherited from the base (0.95) and this pair is BELOW it — normal churn on this source may be higher than Bellingham's; ASSUMPTION — requires an approved per-source floor before the failure is treated as a regression.",
    "FACT — FILL (lambda-20260825-202507-c7532c38_2026-08-25.json): 18 fields populated in 100% of records (AgentName, AgentID, LegacyAgentID, Jurisdiction, ProjectCode, Title, Description, BidURL, PublishedDate, DueDate, ContactName, ContactPhone, ContactEmail, ResourceURL, BidDocuments, BidDocumentHashes, Created, Refreshed); 26 fields empty in 100% (BidType, ContractValue, AwardDate, IssuingAgencyAddress1, IssuingAgencyAddress2, IssuingAgencyCity, IssuingAgencyState, IssuingAgencyZip, ConferenceDescription, ConferenceInfo, Addendum, ContactTitle, ContactFax, AwardedVendorName, AddendumDocuments, AddendumDocumentHashes, BidTabulations, BidTabulationHashes, AwardDocuments, AwardDocumentHashes, UsingAgencyAddress1, UsingAgencyAddress2, UsingAgencyCity, UsingAgencyState, UsingAgencyZip, Modified). Deliberately not excluded: a field that starts carrying data is information, not noise.",
    "FACT — TITLES: 0 groups of 3 or more records share an identical Title in the newest run. Title is not a key and must never drive dedupe (rule 5); this figure is the baseline for the GTZenda duplicate-titles alert.",
    "VALIDATION WITHHELD: no validation block until the observed formats are verified against the engine's parsers with zero false positives, as was done for Bellingham v7. Observed samples — DueDate ['09/04/2026', '09/08/2026'], PublishedDate ['08/24/2026', '08/25/2026'], AwardDate []."
  ]
}
